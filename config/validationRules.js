// const { check, validationResult } = require("express-validator");
//
// const validationRules = [
//   check("user").isLength({ min: 2 }).withMessage("Username must be at least 2 characters long"),
//   check("pwd").isLength({ min: 8 }).withMessage("Password must be at least 8 characters long")
// ];
//
// module.exports = validationRules
//
